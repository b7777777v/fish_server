# 環境變數範本文件
# 複製此文件為 .env.dev, .env.staging, .env.prod 並填入對應的值

# 基本環境設定
ENVIRONMENT=dev
FISH_ENVIRONMENT=dev

# 數據庫配置
DB_USER=user
DB_PASSWORD=password
DATABASE_URL=postgresql://user:password@postgres:5432/fish_db?sslmode=require

# Redis 配置
REDIS_ADDR=redis:6379
REDIS_PASSWORD=

# JWT 配置
JWT_SECRET=your-super-secret-key-change-this-in-production

# Pprof 認證（僅在需要時使用）
PPROF_AUTH_KEY=pprof-auth-secret-key

# 日誌級別
LOG_LEVEL=info

# Gin 模式
GIN_MODE=release